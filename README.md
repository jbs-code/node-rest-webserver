# Node-Rest-WebServer

## Follow the steps

1. Clone ".env.template" and rename as ".env" and put the variables.
2. Install the dependencies, run:

```
pnpm install
```

3. Start app, run:

```
pnpm run dev;
```

4. Start database, run:

```
docker compose up -d;
```
